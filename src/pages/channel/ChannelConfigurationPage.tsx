import PlaceholderPage from "@/pages/PlaceholderPage";
export default function ChannelConfigurationPage({ channelName }: { channelName: string }) {
  return <PlaceholderPage title={`${channelName} Configuration`} description={`Configure your ${channelName} sales channel.`} />;
}
